ALGORITHM algorithm_name
VAR
    sentlength := 0
    c : string
    vowel-count := 0
BEGIN
    
    REPEAT
       read(c)
sentlength=sentlength+1
    UNTIL (c=".")
    IF (c=" ") THEN
       word-count = word-count+1
    END_IF
    IF (c in [e,i,o,a,u]) THEN
        vowel-count=vowel-count+1
    END_IF
END
